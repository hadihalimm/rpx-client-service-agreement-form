interface Province {
  code: string;
  name: string;
}

interface City {
  code: string;
  name: string;
}

interface SubDistrict {
  code: string;
  name: string;
}

interface Village {
  code: string;
  name: string;
  postal_code: string;
}

interface Sales {
  salesId: string;
  salesName: string;
  email: string;
}
